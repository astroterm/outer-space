---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: metrics-server
spec:
  interval: 1h
  chartRef:
    kind: OCIRepository
    name: metrics-server
    namespace: flux-system
  valuesFrom:
    - kind: ConfigMap
      name: metrics-values
