apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: astro-cert
  namespace: networking
spec:
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - "*.astro1.dev"
    - "astro1.dev"
  secretName: astro-cert