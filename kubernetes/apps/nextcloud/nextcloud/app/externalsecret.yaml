apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: nextcloud-secret
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: infisical

  target:
    name: nextcloud-secret
    creationPolicy: Owner
    template:
      engineVersion: v2
      data:
        nextcloud-username: "{{ .username }}"
        nextcloud-password: "{{ .password }}"
        redis-password: "{{ .redis }}"
        mariadb-root-password: "{{ .mariadb }}"
        mariadb-replication-password: "{{ .mariadb }}"
        mariadb-password: "{{ .mariadb }}"
        


  dataFrom:
    - extract:
        key: nextcloud