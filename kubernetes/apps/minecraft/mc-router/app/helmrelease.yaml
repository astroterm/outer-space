---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: mc-router
spec:
  interval: 1h
  chart:
    spec:
      chart: itzg
      version: 1.4.1
      sourceRef:
        kind: HelmRepository
        name: mc-router
        namespace: flux-system
  valuesFrom:
    - kind: ConfigMap
      name: mc-router-values
