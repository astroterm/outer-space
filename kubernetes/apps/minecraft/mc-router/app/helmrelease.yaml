---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: mc-router
spec:
  interval: 1h
  chart:
    spec:
      chart: mc-router
      version: 1.4.1
      sourceRef:
        kind: HelmRepository
        name: itzg
        namespace: flux-system
  valuesFrom:
    - kind: ConfigMap
      name: mc-router-values
