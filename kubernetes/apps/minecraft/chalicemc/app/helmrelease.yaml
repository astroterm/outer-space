---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: chalicemc
spec:
  interval: 1h
  chart:
    spec:
      chart: minecraft
      version: 5.1.3
      sourceRef:
        kind: HelmRepository
        name: itzg
        namespace: flux-system
  valuesFrom:
    - kind: ConfigMap
      name: chalicemc-values
