---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: longhorn
spec:
  interval: 1h
  chartRef:
    kind: OCIRepository
    name: longhorn
    namespace: flux-system
  valuesFrom:
    - kind: ConfigMap
      name: longhorn-values
